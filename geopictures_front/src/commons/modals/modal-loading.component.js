import React from 'react'
import LoadingView from "../component/loading.component";

const ModalLoading = ({ modal: { closeModal  }}) => {
        return <LoadingView color={"white"}/>;
};

export default ModalLoading;
